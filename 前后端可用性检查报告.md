# 知舆项目前后端可用性检查报告

> 检查时间：2026-09-01
> 检查范围：后端 FastAPI 服务、前端 HTML 页面、MIROFISH / MITA 适配、依赖与环境
> 检查方式：代码审查 + 实际启动验证 + API 端点测试

---

## 一、总体结论

**核心前后端可以正常运行，基础功能可用。** 后端 FastAPI 服务能成功启动（31个路由），三个前端页面（版本选择页 / 个人版 / 企业版）均能正常访问，用户注册/登录、数据库、认证系统工作正常。

但存在以下需要注意的问题：

| 类别 | 状态 | 说明 |
|------|------|------|
| 后端核心服务 | ✅ 可用 | FastAPI 启动正常，数据库自动建表，JWT 认证可用 |
| 前端页面 | ✅ 可用 | 三个页面均能访问，静态资源加载正常 |
| 个人版功能 | ⚠️ 部分可用 | API 对接较完整（7个 fetch 调用），但需逐一验证 |
| 企业版功能 | ⚠️ 大量静态展示 | 仅 2 个直接 fetch 调用，多数功能模块为前端静态展示 |
| MIROFISH 适配 | ❌ 未验证通过 | 客户端代码完整，但依赖外部服务（localhost:5001）未运行 |
| MITA 适配 | ❌ 代码中不存在 | 全项目搜索无任何 MITA 相关引用 |
| 依赖环境 | ⚠️ 过于臃肿 | requirements.txt 含 torch/selenium/playwright 等重型依赖，核心功能不需要 |

---

## 二、本次检查中已修复的问题

以下两个问题是此前文件夹整理时移动文件导致的，已在本次检查中修复：

### 修复 1：后端启动失败（index_files 目录不存在）

- **问题**：`app/main.py` 第136行 `StaticFiles(directory=ROOT_DIR / "index_files")`，但 `index_files/` 目录已被移至归档文件夹，FastAPI 的 StaticFiles 在目录不存在时直接抛 `RuntimeError`，导致后端完全无法启动。
- **修复**：改为条件挂载——目录存在时才挂载，不存在则跳过。
- **验证**：修复后 `from app.main import app` 导入成功，31个路由正常注册。

### 修复 2：首页访问 500（desktop.html 不存在）

- **问题**：`app/main.py` 中根路径 `/` 和 `/personal`、`/enterprise` 的 fallback 都指向 `desktop/desktop.html`，但 `desktop/` 目录已被移走。
- **修复**：全部改为指向 `web/landing.html`（版本选择首页）。
- **验证**：访问 `/` 返回 200，页面标题为"知舆 ZhiYu - AI驱动的舆情智能分析平台"。

---

## 三、后端可用性评估

### 3.1 启动与基础服务

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 模块导入 | ✅ 通过 | `app.main` 导入成功，无语法/导入错误 |
| 服务启动 | ✅ 通过 | uvicorn 启动成功，监听 8000 端口 |
| 健康检查 | ✅ 通过 | `GET /health` 返回 `{"status": "ok"}` |
| 数据库初始化 | ✅ 通过 | 启动时自动建表 + 迁移（SQLite） |
| Swagger 文档 | ✅ 通过 | `GET /docs` 可访问，API 文档自动生成 |

### 3.2 API 端点测试

| 端点 | 方法 | 结果 | 说明 |
|------|------|------|------|
| `/auth/register` | POST | ✅ 成功 | 返回 JWT access_token，用户写入数据库 |
| `/auth/login` | POST | ✅ 可用 | 已注册用户可登录；不存在的用户返回 400（正常） |
| `/health` | GET | ✅ 成功 | 基础健康检查 |
| `/api/simulation/full` | POST | ⚠️ 调用成功但业务失败 | API 路由正常，MIROFISH 服务未运行导致业务失败（有错误处理，不崩溃） |

### 3.3 路由结构

共注册 **31 个路由**，分布在 23 个 API 模块中。需要注意的是**路由前缀不统一**：

- **无 `/api` 前缀**：`/auth`、`/bias`、`/care`、`/chat`、`/history`、`/hotspot`、`/legal`、`/media`、`/monitor`、`/notifications`、`/plan`、`/predict`、`/profile`、`/settings`、`/videos`
- **有 `/api` 前缀**：`/api/critical-thinking`、`/api/enterprise`、`/api/report`、`/api/search`、`/api/simulation`

前端调用的路径与后端路由是匹配的，不影响运行，但建议后续统一。

### 3.4 外部服务配置

`.env` 中已配置以下外部服务密钥：

| 服务 | 配置状态 | 用途 |
|------|----------|------|
| 阿里云百炼 DashScope | ✅ 已配置 | AI 对话、文本分析（核心依赖） |
| Coze 工作流 | ✅ 已配置 | 报告生成 |
| 秘塔搜索 METASO | ✅ 已配置 | 网络搜索 |
| MIROFISH 模拟服务 | ⚠️ 仅配置了 URL | `http://localhost:5001`，服务本身未运行 |

---

## 四、前端可用性评估

### 4.1 页面访问

| 页面 | 路由 | 大小 | 访问状态 |
|------|------|------|----------|
| 版本选择页 | `/` | 4,410 字节 | ✅ 200 |
| 个人版 | `/personal` | 28,945 字节 | ✅ 200 |
| 企业版 | `/enterprise` | 67,704 字节 | ✅ 200 |

### 4.2 静态资源

`web/shared/` 目录结构完整，所有文件均存在：

```
web/shared/
├── components/
│   ├── auth-modal.js        (5,286 字节)
│   ├── owl-chat.js          (7,107 字节)
│   └── version-switcher.js  (5,333 字节)
├── css/
│   ├── components.css       (8,633 字节)
│   └── theme.css            (4,739 字节)
└── js/
    ├── api-client.js        (2,793 字节)
    └── store.js             (2,240 字节)
```

三个页面都正确引用了 `/web/shared/js/api-client.js`、`/web/shared/js/store.js` 和 `/web/shared/components/version-switcher.js`。

### 4.3 个人版（personal.html）

- **5 个 script 标签**：Chart.js CDN + 3 个 shared JS + 内联脚本
- **7 个直接 fetch 调用**：`/auth/login`、`/auth/me`、`/auth/register`、`/bias/analyze`、`/history/`、`/media/check`
- **8 次 ZhiYuAPI 引用**：通过统一 API 客户端调用其他接口
- **评估**：API 对接相对完整，核心功能（信源分析、观点对冲、历史记录）有后端支撑。

### 4.4 企业版（enterprise.html）

- **5 个 script 标签**：ECharts CDN + 3 个 shared JS + 内联脚本
- **仅 2 个直接 fetch 调用**：`/auth/login`、`/auth/me`
- **0 次 ZhiYuAPI 引用**：没有通过统一 API 客户端调用业务接口
- **评估**：企业版页面虽然有完整的 UI 布局（仪表盘、监测、预警、报告等模块），但**绝大多数功能是前端静态展示**，没有对接后端 API。这是最大的功能缺口。

---

## 五、MIROFISH 适配情况

### 5.1 代码完整性

| 文件 | 大小 | 状态 |
|------|------|------|
| `app/services/mirofish_client.py` | 9,523 字节 | ✅ 代码完整 |
| `app/api/simulation.py` | 3,715 字节 | ✅ 路由完整 |

`mirofish_client.py` 封装了完整的调用链：
- `prepare_simulation()` → POST `/api/simulation/prepare`
- `run_simulation()` → POST `/api/simulation/run/{id}`
- `get_report()` → GET `/api/report/{id}`
- `full_pipeline()` → 一键全流程（准备→运行→轮询获取报告）

包含连接错误、超时、HTTP 错误的重试和异常处理，不会导致后端崩溃。

### 5.2 未验证通过的原因

1. **MIROFISH 服务未运行**：客户端硬编码连接 `http://localhost:5001`，但该端口没有服务在运行。实际测试调用返回"无法连接 MIROFISH 服务"。
2. **MIROFISH 是独立的外部服务**：需要单独部署（Docker 容器或独立 Python 服务），不在本项目代码库中。归档目录 `_archive_not_for_upload/unrelated_projects/MiroFish/` 下有一个 MiroFish 项目，但不确定是否就是对应的模拟服务。
3. **配置硬编码**：`mirofish_client.py` 第22行 `MIROFISH_BASE = "http://localhost:5001"` 是硬编码的，没有使用 `config.py` 中的 `settings.mirofish_base_url`。修改 `.env` 中的 `MIROFISH_BASE_URL` 不会生效。

### 5.3 结论

MIROFISH 适配的**客户端代码和 API 路由是完整的、写好了的**，但因为依赖的外部服务没有运行，所以**端到端功能没有验证通过**。如果启动对应的 MIROFISH 服务，这部分功能应该可以工作。

---

## 六、MITA 适配情况

**在整个知舆项目代码库中，未找到任何 MITA 相关的代码、配置或引用。**

- 搜索范围：`app/`、`web/`、`scripts/`、根目录所有 `.py`、`.js`、`.html`、`.env`、`.md` 文件
- 搜索关键词：`MITA`、`mita`、`Mita`
- 结果：0 个匹配

**可能的原因：**
1. MITA 适配代码写在了其他项目中（如 MiroFish 独立项目），没有合并到知舆代码库
2. 适配只做了规划或局部尝试，没有提交到代码库
3. 名称记忆有偏差，实际适配的是其他服务

**建议**：确认 MITA 具体指什么服务、适配代码在哪里。如果确实需要这个功能，需要补充开发。

---

## 七、依赖与环境问题

### 7.1 requirements.txt 过于臃肿

当前 `requirements.txt` 包含 **40+ 个包**，其中很多是核心功能不需要的重型依赖：

| 依赖 | 大小估算 | 用途 | 核心功能是否需要 |
|------|----------|------|------------------|
| torch + torchvision + torchaudio | ~2 GB | 多模态模型训练/推理 | ❌ 不需要（AI 调用走百炼 API） |
| transformers | ~500 MB | HuggingFace 模型 | ❌ 不需要 |
| opencv-python | ~200 MB | 视频帧处理 | ⚠️ 视频情绪分析可能需要 |
| librosa | ~100 MB | 音频处理 | ⚠️ 视频情绪分析可能需要 |
| selenium | ~100 MB | 浏览器自动化 | ❌ 不需要（爬虫可用 httpx） |
| playwright | ~200 MB（含浏览器） | 浏览器自动化 | ❌ 不需要 |
| vosk | ~50 MB（+模型 ~1.5GB） | 离线语音识别 | ⚠️ 视频情绪分析需要 |
| scikit-learn | ~100 MB | 机器学习 | ❌ 不需要 |
| pytesseract | ~10 MB | OCR | ❌ 不需要（需额外安装 tesseract 二进制） |

**核心功能实际只需要约 15 个包**：fastapi、uvicorn、python-multipart、sqlalchemy、aiosqlite、pydantic-settings、python-jose、passlib、python-dotenv、httpx、dashscope、loguru、requests、beautifulsoup4、aiofiles、python-docx、pypdf、Pillow、numpy、pandas。

**影响**：新环境执行 `pip install -r requirements.txt` 会下载 3GB+ 的依赖，耗时 30 分钟以上，且 torch/transformers 在 Python 3.14 上可能编译失败。

### 7.2 Python 版本

- `.venv` 虚拟环境使用 **Python 3.14.3**（非常新的版本）
- 系统 Python 是 3.13.13
- `simulation.py` 的注释中提到"避免 pydantic-core 在 Python 3.14 上的编译问题"，说明已经遇到过兼容性问题
- **建议**：使用 Python 3.11 或 3.12，兼容性更好

### 7.3 vosk 模型

- 语音识别需要 Vosk 中文模型（`models/vosk-model-cn-0.22/`，约 1.5GB）
- `models/` 目录已被 `.gitignore` 排除，不会上传到 GitHub
- 新用户需要单独下载模型才能使用视频情绪分析功能

---

## 八、其他发现

### 8.1 .env 包含真实密钥

`.env` 文件中包含真实的 API Key（DashScope、Coze、秘塔搜索），已被 `.gitignore` 排除，不会上传到 GitHub。但请注意不要手动将 `.env` 提交到仓库。

### 8.2 企业版功能缺口最大

企业版页面（67KB）有完整的 UI 布局，包括仪表盘、实时监测、风险预警、传播分析、报告生成、策划分析等模块，但**只有登录功能对接了后端 API**，其余模块均为静态展示。如果要让企业版真正可用，需要补充大量前后端对接工作。

### 8.3 预测服务（predict_app.py）

项目根目录有一个 `predict_app.py`（Flask，端口 5000），是独立的热度预测服务。它不是主后端的一部分，需要单独启动。主后端的 `/predict` 路由可能调用这个服务。

---

## 九、修复建议与优先级

### 高优先级（影响基本运行）

| # | 问题 | 建议 | 状态 |
|---|------|------|------|
| 1 | index_files 目录不存在导致启动失败 | 改为条件挂载 | ✅ 已修复 |
| 2 | 首页指向不存在的 desktop.html | 改为指向 landing.html | ✅ 已修复 |
| 3 | requirements.txt 过于臃肿 | 拆分为 `requirements.txt`（核心）和 `requirements-extra.txt`（多模态/爬虫可选） | 待执行 |

### 中优先级（影响功能完整性）

| # | 问题 | 建议 |
|---|------|------|
| 4 | MIROFISH 服务未运行 | 确认 MIROFISH 服务的部署方式，启动服务后验证端到端功能 |
| 5 | mirofish_client.py 硬编码 URL | 改为读取 `config.py` 的 `settings.mirofish_base_url` |
| 6 | MITA 适配不存在 | 确认 MITA 是什么、代码在哪里，必要时补充开发 |
| 7 | 企业版大量功能为静态展示 | 逐步补充前后端 API 对接（监测、预警、报告等） |

### 低优先级（优化建议）

| # | 问题 | 建议 |
|---|------|------|
| 8 | 路由前缀不统一（有/无 /api） | 后续重构时统一为 `/api/` 前缀 |
| 9 | Python 3.14 兼容性风险 | 建议使用 Python 3.11/3.12 |
| 10 | 模拟推演 API 返回中文乱码 | 检查响应编码（可能是 PowerShell 显示问题，非代码问题） |

---

## 十、快速启动验证步骤

如果你想自己验证前后端是否可用：

```bash
# 1. 进入项目目录
cd C:\Users\fomorou\Desktop\知舆

# 2. 激活虚拟环境
.venv\Scripts\activate

# 3. 启动后端
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000

# 4. 浏览器访问
#    首页（版本选择）: http://127.0.0.1:8000/
#    个人版:          http://127.0.0.1:8000/personal
#    企业版:          http://127.0.0.1:8000/enterprise
#    API 文档:        http://127.0.0.1:8000/docs
```

---

*报告结束*

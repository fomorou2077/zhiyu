# ============================================================
# 知舆 ZhiYu - 其他可选依赖
# 按需安装，核心功能不需要
# ============================================================

# 独立热度预测服务（predict_app.py，Flask 端口 5000）
flask>=3.0.0
flask-cors>=4.0.0

# 图片 OCR（file_processor.py 中图片文字提取）
# 注意: 需额外安装 tesseract 二进制文件
pytesseract>=0.3.0

# API 测试脚本（scripts/test_api.py）
aiohttp>=3.9.0
